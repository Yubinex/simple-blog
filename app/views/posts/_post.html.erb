<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h6 style='opacity: 0.6;'>
    posted by <%= (post.user.name.present? ? post.user.name : post.user.email) %>
  </h6>
  <div>
    <%= post.body %>
  </div>
</div>
